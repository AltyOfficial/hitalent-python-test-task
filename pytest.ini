[pytest]
DJANGO_SETTINGS_MODULE = server.settings
python_files = test_*.py
addopts = -v --strict-markers --tb=short --maxfail=5
markers = 
    django_db: mark test as needing django database
    slow: mark test as slow